rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Tüm kullanıcılar okuyabilir, sadece giriş yapanlar yazabilir
    match /{document=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
